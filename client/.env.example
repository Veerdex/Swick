# URL of the SWICK Socket.io server. Defaults to the local dev server if unset.
VITE_SERVER_URL=http://localhost:3001

# Supabase (player auth). URL + anon key are public — safe in the client bundle.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
